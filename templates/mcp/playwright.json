{
  "_comment": "Playwright MCP — browser automation for Claude. Lets Claude navigate, click, screenshot, and test web UIs.",
  "_install": "npx playwright install chromium",
  "_usage": "Merge the 'playwright' block below into your mcpServers section in ~/.claude/settings.json",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    }
  }
}
